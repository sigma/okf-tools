---
title: A Clean Page
type: idea
status: draft
created: 2026-07-20
tags: [alpha, beta]
description: A page whose frontmatter matches the schema exactly.
---

A page whose frontmatter is fully schema-conformant: `title` feeds the derived
Name column, `type` is a derived column (ignored), and every other key is a
declared `source: frontmatter` column with a valid value.
